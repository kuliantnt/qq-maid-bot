{
  "name": "qq-maid-web-console",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "node scripts/build.mjs",
    "check:demo": "tsc --noEmit --project tsconfig.demo.json",
    "build:demo": "node scripts/build-demo.mjs",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
